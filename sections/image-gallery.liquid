<div class="gallery-section">
  <div class="page-width">
     <div class="image-gallery zoom-gallery">
         {% for block in section.blocks %}
            <a href="{{ block.settings.image-gallery }}" title="{{ block.settings.image-gallery.alt }}" data-source="http://500px.com/photo/32736307"  style="width:193px;height:125px;">
             <img src="{{ block.settings.image-gallery |  image_url: width:1024 }}" width="1024" height="1024" alt="{{ block.settings.image-gallery.alt }}" loading="lazy" />
            </a>
        {% endfor %}   
     </div>
  </div>
</div>

<script>
    $(document).ready(function() {
	$('.zoom-gallery').magnificPopup({
		delegate: 'a',
		type: 'image',
		closeOnContentClick: false,
		closeBtnInside: false,
		mainClass: 'mfp-with-zoom mfp-img-mobile',
		image: {
			verticalFit: true,
			titleSrc: function(item) {
				return item.el.attr('title') + ' &middot; <a class="image-source-link" href="'+item.el.attr('data-source')+'" target="_blank">image source</a>';
			}
		},
		gallery: {
			enabled: true
		},
		zoom: {
			enabled: true,
			duration: 300, // don't foget to change the duration also in CSS
			opener: function(element) {
				return element.find('img');
			}
		}
		
	});
});
</script>

{% schema %}
{
  "name":"Image Gallery",
  "class":"image-pop-gallery",
  "blocks":[
    {
        "type":"image_picker",
        "name":"Image",
        "settings":[
            {
                "type":"image_picker",
                "id":"image-gallery",
                "label":"Image"
            }
        ]
    }
  ],
  "presets":[
    {
      "name":"Image Gallery"
    }
  ]
}
{% endschema %}    